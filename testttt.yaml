
version: 0.2
phases:
  install:
    commands:
      # Install Python and dependencies
      - apt-get update && apt-get install -y python3 python3-pip
      - pip3 install coverage  # Required for test coverage analysis
      
      # Install SonarScanner
      - wget https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-5.0.1.3006-linux.zip
      - unzip sonar-scanner-*.zip -d /opt
      - export PATH="/opt/sonar-scanner-5.0.1.3006-linux/bin:$PATH"

      - pip3 install psycopg2-binary coverage


  pre_build:
    commands:
      # Run unit tests with coverage tracking
      - coverage run -m unittest discover -s . -p "test_*.py"
      - coverage report  # Debug coverage
      
      - docker run -d --name test-db -e POSTGRES_PASSWORD=testpass -p 5432:5432 postgres:13
      - sleep 10  # Wait for DB to initialize
      # Run migrations (example)
      - psql -h localhost -U postgres -c "CREATE DATABASE testdb;"


  build:
    commands:
      # Generate XML coverage report
      - coverage xml
      
      -Dsonar.projectKey=database-handler


      # Run SonarQube analysis with coverage
      - >-
        sonar-scanner
        -Dsonar.projectKey=amr-test
        -Dsonar.host.url=http://18.117.119.200:9000
        -Dsonar.login=sqp_7d760d846bc1b7bf46098443339c4ef823017908
        -Dsonar.sources=.
        -Dsonar.python.coverage.reportPaths=coverage.xml
      
  post_build:
    commands:
      - docker stop test-db
      - docker rm test-db


