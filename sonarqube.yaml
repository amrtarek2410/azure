version: 0.2
phases:
  install:
    commands:
      - wget https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-5.0.1.3006-linux.zip
      - unzip sonar-scanner-*.zip -d /opt
      - export PATH="/opt/sonar-scanner-5.0.1.3006-linux/bin:$PATH"
  build:
    commands:
      # Multi-line command with quotes
      - >-
        sonar-scanner
        -Dsonar.projectKey=amr-test   #project name
        -Dsonar.host.url=http://3.146.35.25:9000    #add public ip xxxxx:9000
        -Dsonar.login=sqp_c97cc9a515e7643898cb47c5ae01eff9e1087e1b     # token from sonarqube
        -Dsonar.sources=.
